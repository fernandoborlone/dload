{
  "name": "dloadtest",
  "version": "1.0.0",
  "description": "Repositório de testes End-to-end, Testes de API e stress test, utilizando o Cypress e JMeter",
  "main": "index.js",
  "scripts": {
    "test:open_dev": "npx cypress open --env configFile=dev",
    "test:run_dev": "npx cypress run --env configFile=dev --reporter mochawesome"

  },
  "keywords": [
    "cypress",
    "performance",
    "e2e",
    "api"
  ],
  "author": "Fernando Borlone",
  "license": "MIT",
  "devDependencies": {
    "cypress": "^12.7.0",
    "cypress-mochawesome-reporter": "^3.3.0",
    "faker-br": "^0.4.1"
  }
}
